// No schema needed for PostgreSQL. This file can export helper functions if needed.
// Structure reference for SQL:
// id (SERIAL PRIMARY KEY), workflow_id (INTEGER), order (INTEGER), title (TEXT), description (TEXT), website_url (TEXT), required_documents (TEXT[]), action_checklist (TEXT[]), created_at (TIMESTAMP)

// Example: module.exports = { ...helper functions if needed... };
